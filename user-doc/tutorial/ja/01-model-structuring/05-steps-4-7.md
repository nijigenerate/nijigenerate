# 05. 手順 4-7

## ステップ 4：Node / Composite / DynamicComposite を「親として挿入」する

ここからは、補助用のノードを追加していきます。  
大事なルールは、「既存の子を別のツリーに引っ越さず、**親として挿入する**」ことです。  
特に、**「動かしたい Part を選んで、その親として挿入する」**のを基本とします。

Node / Composite / GridDeformer は、挿入時に「選択していたオブジェクトの原点」を引き継ぎます。  
そのため、上位の Node を選んで挿入すると、原点が大きくずれてしまい、回転や変形の基準が取りづらくなります。

**迷ったときの選び方**

- 親を 1 段「差し込みたい」→ **挿入（Insert）**
- 種類が違うのを「置き換えたい」→ **変換（ConvertTo）**
  - Node → DynamicComposite の直後は `autoResizedMode` を **2 回トグル**
  - 目だけでなく口も DynamicComposite にして、表情差分のルールを揃える

**用途の目安**

- 目・口の差分管理 … `Composite` または `DynamicComposite` を「親として挿入」
- 腕・脚の分節 … 起点となる Part を適切に選び、その親として `Node` を挿入する

**具体的な操作例**

- 目の Part をまとめて選択 → 「親として Composite 挿入」→ `Eyes::Comp` などの名前を付ける
- 腕の付け根の Part を選択 → 「親として Node 挿入」→ `Arm R::Joint` のように名前を付ける

## ステップ 5：GridDeformer を「必要な場所だけ」に入れる

GridDeformer はとても強力ですが、たくさん入れすぎると管理が大変になります。  
基本は、次の部分にだけ入れます。

- 顔
- 後頭部
- 横髪
- 胴体
- 前髪
- 胸（胸が別パーツのとき）

顔や胴体のような広い面は、Part ひとつひとつにメッシュを張るだけでは扱いづらくなります。  
先に `GridDeformer` で変形用のまとまりを作っておくと、表情や体の揺れを後から自然に調整しやすくなります。

**手順の目安**

- すでに同じ位置に「まとめ用の `Node`」が入っていて、それをグリッドにしたい場合は、その `Node` を `GridDeformer` に **ConvertTo（変換）** して置き換える
- まとめ用の `Node` が無い場合は、変形させたい **Part** を選択し、その **親として GridDeformer を挿入**する
- GridDeformer 自体には通常の AutoMesh は掛けない
- 広い面を動かす予定がある場合は、Part に AutoMesh を掛ける前に、その外側に `GridDeformer` を用意しておく

格子数の目安としては、

- 顔や後頭部 … **8×8 程度**
- 胴体 … **10×14 程度**

をベースに、キャラクターの縦横比やディテールに合わせて調整します。

また Grid のマージンは **全体サイズの約 10% 前後** を目安にすると、変形時にエッジが自然に変形しやすくなります。

### 補足：目・口の DynamicComposite とグリッド

目や口など、顔の差分管理に `DynamicComposite` を使う場合、グリッドにしておくと変形の影響が見えやすく、調整がしやすくなります。  
まずは **6×6〜8×8 程度**を目安にして、必要に応じて増減してください。

**具体的な操作例**

- 顔の Part を選択 → 「親として GridDeformer 挿入」→ `Face::G` などの名前を付ける
- `Face::G` の子に `Face` Part が残っていることを確認する
- 同様に、後頭部・横髪・胴体・前髪・胸部にも必要に応じて GridDeformer を挿入する

## ステップ 6：前髪の Grid と胸パーツ、衣装の配置

少しだけ応用として、前髪と胸パーツ、衣装の置き方を整えます。

**前髪専用 Grid**

- 顔とは別に、前髪だけを大きく変形させたい場合に使う
- 顔の子として `FrontHair::G (GridDeformer)` を置き、その下に前髪の Part をぶら下げる

**胸部が別パーツの場合**

- 胴体に GridDeformer を追加し、その子として胸部 Part を配置する
  → 胴体のゆれや変形に胸も一緒に追従しやすくなります

**衣装（スカートなど）**

- スカートなど体についた衣装パーツは、胴体の子に置く
- こうすることで、体と一緒に移動しつつ、必要に応じて衣装だけ回転・変形させることができます

## ステップ 7：AutoMesh の設定と一括操作

ツリー構成がある程度できたら、メッシュを張っていきます。

**基本ルール**

- `Part` … 基本は `Optimum` アルゴリズムで AutoMesh
- `GridDeformer` … Grid 用 AutoMesh（Grid プロセッサ）を使って **Grid で AutoMesh**

**仕上げの流れ**

1. 顔・胴体・前髪など、まとめて変形したい範囲に `GridDeformer` を入れる
2. `GridDeformer` の子に、変形させたい Part 群が入っていることを確認する
3. `GridDeformer` には Grid 用 AutoMesh を掛ける
4. 各 `Part` には `Optimum` を掛ける

この順番にすると、「広い面を動かすためのグリッド」と「各パーツの形に沿ったメッシュ」を分けて管理できます。

**よくある失敗**

- GridDeformer に通常の Optimum メッシュを掛けてしまう
- 逆に、Part に Grid 用の設定を使ってしまう
- 顔や胴体などの広い面に `GridDeformer` を入れ忘れ、あとから表情や揺れを付けにくくなる
- 格子状に動かしたい範囲と、パーツ形状に沿わせたい範囲を同じ AutoMesh 設定で処理してしまう

**複数パーツの一括操作（コンテキスト選択）**

- 同じようなパーツに同じ AutoMesh 設定を掛けたいときはもちろん、**ノードの挿入や追加、複数の子ノードを同じ親ノードに移す場合**なども、一括操作が便利です。
- 複数の Part / Node を選択してから操作すると、AutoMesh や親変更をまとめて適用できます。
- まとめて操作したあとは、意図した対象だけが変わっているかツリー上で確認しておくと安心です。

## 次に読む

- 最終確認と参考ツリーを見る  
  → [06. チェックポイントと参考ツリー](./06-checklist-and-tree.md)
